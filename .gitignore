# Build output
dist/
build/
build-cmake/
build-qt/
.build/
DerivedData/
*.xcarchive
*.dmg

# Xcode per-user state
xcuserdata/
*.xcuserstate

# macOS
.DS_Store

# Signing material and secrets — never commit
*.p12
*.p8
*.cer
*.certSigningRequest
.env

# Claude Code settings personal to this machine
.claude/settings.local.json
BUILD_PLAN.md

# Flatpak build artifacts (sources are fetched by flatpak-builder at build time)
.flatpak-builder/
build-dir/
build-flatpak/
